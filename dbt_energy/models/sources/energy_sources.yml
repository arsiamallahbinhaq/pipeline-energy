version: 2

sources:
  - name: raw
    schema: raw
    tables:
      - name: raw_energy
        description: Raw energy data from OWID
        freshness:
          warn_after:
            count: 7
            period: day
